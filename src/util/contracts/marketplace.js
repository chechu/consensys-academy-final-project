import { getUport } from '../connectors'

const abi = require('./abi/Marketplace.json').abi;
const contractAddress = '0xe11E56e18d34f591bAD7Dd888127976D5Af9a79E';
// const marketplaceContract = getUport().contract(abi).at(contractAddress);

// export default marketplaceContract;
